/**
 * As basic of a description as you can get. 
 * Displays the same description every time.
 * @param {string} standard Standard description
 */
export default function Basic(standard) {
    this.standard = standard;
}